version: '3.8'
services:
  db:
   image: mysql:8.0
   container_name: ${CONTAINER_NAME}
   environment:
     MYSQL_ROOT_PASSWORD: ${ROOT_PASSWORD}
     MYSQL_DATABASE: ${DATABASE}
   ports:
    - "3306:3306"
   env_file:
    - .env
# volumes